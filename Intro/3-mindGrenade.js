const num1 = 5;
const num2 = 10;

function addValues() {
    console.log(`the sum is: ${num1 + num2}`);
}

addValues();

/*when the function is executed and requires it will run. when require alway it will ba called*/